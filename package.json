{
  "name": "snlib-book-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit",
    "build:test": "tsc --noEmit false --outDir .build"
  },
  "dependencies": {
    "@upstash/redis": "^1.39.0",
    "cheerio": "^1.1.2"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "typescript": "^5.9.3"
  }
}
